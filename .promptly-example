io.write(
    fmt("red", "("),
    env.USER,
    fmt("red", "@"),
    env.HOST,
    fmt("red", ") "),
    fmt("yellow", "bright", env.PWD),
    " ",
    fmt(red, env.USER == "root" and "# " or "$ ")
)

